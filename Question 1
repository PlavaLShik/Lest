# В предоставленном варианте сравнение идет с помощью логической операции AND. 

def evenOdd(n):

    return n & 1 == 0

# В этом варианте идет проверка на четность через рекурсию. 

def checkEvenOdd(n):

   if(n==0):

      return True

   elif(n==1):

      return False

   else:

      return checkEvenOdd(n-2)

# В другом варианте идет сравнение остатка от деления. При использовании больших чисел может стать причиной переполнения

def isEven(n):

      return n % 2 == 0


# % При использовании больших чисел оператор может дать некорректный результат из-за переполнения
# & Смотрит на последний бит, может работать с большими числами
# Recursion Медленнее чем остальные в глубокой рекурсии может вызвать переполнение стека
